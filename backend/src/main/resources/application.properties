# Application
spring.application.name=CampusZen API
server.port=8080

# Database
# Database
spring.datasource.url=${DB_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:BDMAckdQmKacCrhdVQxPnrnGULIKcrEl}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
app.jwt.secret=${APP_JWT_SECRET:campuszen-super-secret-key-change-me-2026}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}
